import { pageMetadata } from "@/lib/site-metadata";

export const metadata = pageMetadata("tojeong");

export default function TojeongLayout({ children }: { children: React.ReactNode }) {
  return children;
}
