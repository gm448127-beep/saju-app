import { pageMetadata } from "@/lib/site-metadata";

export const metadata = pageMetadata("historySaved");

export default function HistorySavedLayout({ children }: { children: React.ReactNode }) {
  return children;
}
